# AI Work Audit — 2026-08-24

## What was verified

Three newer commits had been added to the public release line after the prior full-team slow-motion playbook release. Git records the same GitHub account for all three, so this repository cannot honestly identify which ideas came from Google Antigravity, Grok, or another assistant.

The direction was clear and useful:

- make the site a stronger sideline command center;
- add wake lock, navigation, backup, play clock, and fair-play tools;
- make drills more visual and practice more structured;
- expand the play diagram beyond static cards.

## Useful work retained

- wake-lock and field-navigation conveniences;
- backup and local-state utilities;
- richer practice and drill controls;
- the stronger outdoor visual theme;
- the six large play-selector buttons;
- anonymous public roster defaults.

## Conflicts corrected

- The replacement diagram removed the verified seven-beat kid teacher. The governed slow-motion teacher is restored.
- The replacement formation introduced a split Wide Receiver. The locked five-linemen/three-backs offense is restored.
- A defender was shown head-up over Center. The teaching look now keeps Center and both A gaps clear.
- A drill taught a three-point stance. The app now teaches the safer two-point stance.
- Schedule returned to visible navigation. It is hidden again by owner direction.
- Two competing stylesheet copies could drift. `css/styles.css` is authoritative; the root file is only a compatibility alias.
- A second unused simulator encoded conflicting rules. It was removed from the governed product.

## Durable decision

Jarvis is the project control plane. External AIs are builders or reviewers inside one declared lane; they are not independent product authorities. `PROJECT_AUTHORITY.md`, `AI_CHANGE_PROTOCOL.md`, and `AGENTS.md` define the shared contract, and `scripts/verify_project.mjs` enforces the parts that can be checked automatically.

## Still unknown

- The exact authoring assistant behind each recent commit.
- The current-season Edge/DE alignment interpretation until the league or officials confirm it.
