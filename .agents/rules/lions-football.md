---
description: Required rules for every Lions Flag Football AI change
globs: "**/*"
alwaysApply: true
---

Before editing, read `/AGENTS.md`, `/PROJECT_AUTHORITY.md`, and `/AI_CHANGE_PROTOCOL.md` completely.

Google Antigravity and every other agent are contributors under Jarvis governance, not independent product authorities. Claim the single write lane, use a feature branch, preserve the six-run five-line-three-back system, keep Schedule hidden, keep child identity out of public source, and run `node scripts/verify_project.mjs` before requesting review.

Do not push directly to `main`. Do not replace the verified seven-beat slow-motion playbook with a different simulator unless the owner explicitly approves the migration and the new version passes every acceptance gate.
